"""Data layer -- file I/O and bundled sample datasets.

Reading model points / basis / scenarios / in-force state from files,
writing measurements out, and loading the bundled sample datasets. The
public read_* / write_measurement / sample_data_dir helpers and the
fcf.samples namespace are re-exported from the top-level fastcashflow
namespace.
"""
